import stylex from '@stylexjs/stylex';

export const svgIconStyles = stylex.create({
  host: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    width: 'inherit',
    height: 'inherit',
    fontSize: 'inherit',
  },
});
