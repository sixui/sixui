import stylex from '@stylexjs/stylex';

export const svgIconStyles = stylex.create({
  host: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    width: '1rem',
    height: '1rem',
  },
});
