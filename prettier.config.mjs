/** @type {import("prettier").Config} */
const config = {
  jsxSingleQuote: true,
  singleQuote: true,
  trailingComma: 'all',
  tabWidth: 2,
  plugins: [],
};

// eslint-disable-next-line import/no-default-export
export default config;
